# Opus bounded AEGIS-03 test-isolation subtask

Read AGENTS.md and approved tasks, then implement only safe Mongo test runner. Exclusive ownership: scripts/test_mongo_local.sh and a NEW scripts/test_mongo_runner.test.mjs (or similarly named dedicated test), .agent/outbox/aegis-mongo-runner-result.md. Do not edit other files/manifests/docs/Python source, and no git commit. Other Opus is writing AA source. Use apply_patch via bash ONLY, no subagents/model changes.

Measured bug: current scripts/test_mongo_local.sh registers cleanup before successful startup, connects and shuts down by a fixed port. If port is already occupied it can use/shutdown an unrelated user's Mongo server. Fix it fail-closed: refuse occupied port before startup, only initialize exact child process successfully spawned by this run, use unique generated temp dbpath, verify own PID liveness/startup before initiate, and cleanup signals only owned child process (never shutdown arbitrary server by port). Trap cleanup must never delete any path except exact validated generated temp directory belonging to this run. Handle invalid/empty/nonnumeric/out-of-range TEST_MONGO_PORT, missing binaries, port conflict, startup error, SIGINT, pytest failure. No real user DB connection, no broad destructive commands.

Runner should invoke pytest with -m mongo across tests directory (not only old test_mongo_repository.py), so future AEGIS Mongo tests are included. Tests already use per-test unique DB names. Do not use .env.local or inherited live DB URL; set TEST_MONGODB_URI to ONLY this owned loopback instance. Bind 127.0.0.1 only. Dynamic free port is preferable; explicit port compatibility allowed with safe collision handling. No whole machine process cleanup/kill commands.

Add meaningful dedicated runner safety tests using controlled test executable stubs or loopback temporary process: occupied port preserved/no shutdown command, startup failure cleanup owns only created PID/path, pytest failure exit propagated, invalid port rejected. Test fixture creation also via apply_patch, no shell write tricks for project files. Run bash -n + dedicated tests only. Real full Mongo integration will run once AA implementation is complete; do not execute incomplete Python tests now.

Preserve old P6-03 /Users/vien/MyProjects/PBL-coder untouched. No live network/provider/RPC/AWS/Atlas/keys/actual Mongo access beyond isolated test processes. Report file paths/checks/limits, no commit.
